(set-option :print-success false)
(set-logic QF_LIA)
(declare-fun x () Int)
(declare-fun y () Int)
(declare-fun z () Int)
(assert (= x (+ y z)))
(assert (= x (- y z)))
(check-sat)

(push 1)
(assert (> z 0))
(check-sat)

(pop 1)
(assert (>= z 0))
(check-sat)
(exit)